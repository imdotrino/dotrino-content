{
  "name": "dotrino-content",
  "version": "0.1.0",
  "description": "Nodo de contenido del ecosistema Dotrino: guarda y sirve media pesada por cid (hash de contenido), autohospedado por el usuario. Fase 1: core local (localhost).",
  "type": "module",
  "bin": {
    "dotrino-content": "bin/cli.js"
  },
  "exports": {
    ".": "./src/index.js"
  },
  "scripts": {
    "start": "node bin/cli.js start",
    "test": "node --test 'test/**/*.test.js'",
    "type-check": "tsc --noEmit && tsc --noEmit -p lib"
  },
  "engines": {
    "node": ">=22.5"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "22.20.1",
    "typescript": "5.9.3"
  },
  "dependencies": {
    "@dotrino/identity": "0.55.0",
    "@dotrino/remote-agent": "0.4.1",
    "@dotrino/vault": "0.27.0"
  }
}
