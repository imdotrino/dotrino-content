# No ejecutar lifecycle scripts de dependencias (preinstall/install/postinstall):
# vector #1 de inyección de cadena de suministro.
ignore-scripts=true
# Instalar versiones EXACTAS (sin ^ ni ~). El package-lock.json manda.
save-exact=true
fund=false
